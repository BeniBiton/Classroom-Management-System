import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    formsContainer: {
        paddingTop: "5rem",
    },
    headline: {
        fontSize: "2.75rem",
        display: "flex",
        justifyContent: "center",
        marginBottom: "0.2rem"
    }
}))
