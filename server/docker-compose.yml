version: '3.8'
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: classroom_db
      POSTGRES_PASSWORD: benibiton3
      POSTGRES_DB: classroom_management_db
    ports:
      - '5432:5432'
